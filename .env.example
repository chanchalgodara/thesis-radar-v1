# Vercel Postgres (set in Vercel Dashboard or locally for dev)
# Get from: Vercel project → Storage → Postgres → .env.local
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=

# Optional: Gemini API key (or set in app Settings)
# GEMINI_API_KEY=
