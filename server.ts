// Local production server (npm start). Not used by Vite dev or Vercel deploy.
// This is a standalone Express server for local testing after `npm run build`.
console.log("Use `npm run dev` for development or deploy to Vercel for production.");
console.log("This file is a placeholder — the app uses Vite plugin for dev and api/server.ts for Vercel.");
